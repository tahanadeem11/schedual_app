# Production Environment Variables for cPanel Deployment
# Copy these to your cPanel .env file and update with your actual values

# NextAuth.js Configuration
NEXTAUTH_URL=https://yourdomain.com
NEXTAUTH_SECRET=your-super-secret-key-here-change-this-in-production

# Google OAuth Configuration (for future server-side implementation)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Application Configuration
NODE_ENV=production

# Note: This static build doesn't require these environment variables
# They are included for when you upgrade to server-side rendering
# For now, the app works with mock data and UI-only functionality
